# Archive

This directory stores historical components that are not part of the active runtime/build path.

Current archived items:
- `legacy_cep/` — legacy CEP extension assets kept for historical reference only.

Rules:
- Do not use archived content as a build or deployment source.
- Move deprecated components here with `git mv` instead of deleting permanently.
